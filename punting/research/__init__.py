"""Research acquisition: source access policy, evidence packs, BOM forecasts and research digests.

Everything here feeds the append-only Store through core.validate, so field, race and timing gates apply.
It never places bets, logs in, bypasses paywalls or fetches a path a site's robots.txt disallows.
"""
